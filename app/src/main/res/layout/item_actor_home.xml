<?xml version="1.0" encoding="utf-8"?>
<!--
  item_actor_home.xml
  Actor card for horizontal RecyclerView on the main page.
  Uses CircleImageView for circular actor photos.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="12dp"
    app:cardCornerRadius="12dp"
    app:cardBackgroundColor="@color/surface_dark"
    app:cardElevation="2dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Actor photo (circular) -->
        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/ivActorPhoto"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:contentDescription="@string/cd_actor"
            android:src="@android:drawable/ic_menu_camera"
            app:civ_border_width="2dp"
            app:civ_border_color="?attr/colorOutline"/>

        <!-- Actor name -->
        <TextView
            android:id="@+id/tvActorName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textStyle="bold"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center"
            android:textColor="@color/text_primary"
            android:text="@string/placeholder_actor_name"/>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
